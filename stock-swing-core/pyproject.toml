[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-swing-core"
version = "0.1.0"
description = "Swing trading technical screening MVP"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
